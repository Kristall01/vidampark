stages:
    - build
#    - deploy

build:
    stage: build
    image: gradle:latest
    script:
        - cd backend
        - mkdir sources
        - mv src/* sources
        - mkdir -p src/main/java
        - mv sources/* src/main/java
        - gradle init build
    artifacts:
        paths:
            - backend/build/libs/project.jar

#deploy:
#    stage: deploy
#    script:
#        - mkdir public
#        - mv frontend/src/* public
#    artifacts:
#        paths:
#            - public
#    only:
#        - master